apiVersion: v1
kind: Service
metadata:
    name: nominaltrajectorydemotheolansink
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: nominaltrajectorydemotheolansink